{
    "actorOutputSchemaVersion": 1,
    "title": "PDF Text & Metadata Extractor output",
    "properties": {
        "results": {
            "type": "string",
            "title": "Extracted PDFs",
            "template": "{{links.apiDefaultDatasetUrl}}",
            "description": "One record per PDF URL with the full text, per-page text, optional RAG chunks, metadata, links, word and character counts, or a free failure record with an error type."
        },
        "summary": {
            "type": "string",
            "title": "Run summary",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/SUMMARY",
            "description": "Counts of requested, processed, charged and failed PDFs."
        }
    }
}
